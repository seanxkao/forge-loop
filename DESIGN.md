# 工廠迴圈（暫名 Forge Loop）— 雛型設計文件

> 網頁版增量遊戲。工廠優化 ＋ 做裝 ＋ 自動戰鬥。
> 本文件記錄第一個可玩雛型（最小閉環）的設計共識。數值為起始參考值，之後再平衡。

---

## 1. 核心概念

一句話：**打怪掉素材 → 素材餵機台產出製裝材料 → 反覆製裝、骰詞綴讓英雄變強 → 打更難的關（魔王）拿更多素材 → 製裝更快、骰更多。**

```
        ┌─────────────────────────────────────────────┐
        │                                             │
        ▼                                             │
   ┌─────────┐   掉落原始素材   ┌──────────┐  中間材料  ┌─────────┐
   │  戰鬥    │ ───────────────▶│  生產(方格) │ ────────▶│  製裝    │
   │ 波次關卡 │                  │  機台運轉   │           │ 合成裝備 │
   └─────────┘                  └──────────┘           └─────────┘
        ▲                            │                      │
        │                            │ 消耗素材運轉           │ 裝備
        │        裝備強化英雄          ▼                      │
        └──────────────────────── 英雄 ◀────────────────────┘
```

### 設計引擎：魔王牆 ＋ 抽裝成長（best-of-N）

成長**不**靠「換更高階的裝」，而靠在固定裝備上**反覆骰詞綴、留最好的**：

- 每槽只有**一種固定基底**裝備，沒有上位替代；power 全部來自隨機詞綴（數量 1～4 ＋ 數值）。
- 變強 = 骰到更多、更好的詞綴。做得越多次越可能摸到 god roll，所以**生產速度（製裝次數／分鐘）＝成長速度**。
- 素材只能靠打怪掉；後期關卡掉量呈**數量級暴增**，讓玩家瘋狂製裝衝 roll。
- **瓶頸＝每區魔王**：3 關順順爬，第 4 關魔王數值尖峰卡住你，逼你回去骰更好的裝才能突破。

### 進程原則（平衡依據）

- 總時數約 **1～2 小時**，五區**各約 1/5**（每區時間相近）。
- 每區節奏：3 關快速成長 → 魔王牆停滯 → 骰裝突破。
- power 有**天花板**（固定詞綴池、最多 4 詞 × 3 槽，全 god roll 即上限）；敵人難度曲線對齊此上限回推。
- 生產會滾雪球，故**後期魔王相對更厚**（尖峰倍率逐區加碼）以維持「每區時間相近」。

---

## 2. 核心系統

### 2.1 戰鬥（Combat）

- **單英雄**、**波次關卡**、即時自動戰鬥（玩家不操作單位，戰前決策為主：穿什麼裝、挑哪關）。
- 英雄自動逐波清怪，每隻怪死亡掉落素材；英雄死亡該次中斷。
- **英雄屬性**（皆由裝備推導，裸值極弱）：
  - 生命 `hp`、攻擊 `atk`（點傷加總）、武器本地物理 `localPhysPct`、防禦 `def`、暴擊 `critChance`、暴傷 `critMult`、攻速 `haste`、每秒回血 `hpRegen`、減傷 `dmgReductionPct`、減暴傷承受 `critDmgTakenReductionPct`
- **攻擊公式**（分層，PoE 風）：
  ```
  武器傷害 = (武器基底 + 武器點傷) × (1 + local 物理%)
  總攻擊   = 武器傷害 + 全域點傷（飾品等）
           ×（1 + global 物理%）       ← 未來擴充，現無此詞綴，公式預留插槽
  暴擊時   × 暴傷（critMult）
  命中傷害 = max(1, 總攻擊 − 敵防禦)
  ```
  > `localPhysPct` 為武器**本地**屬性，只乘武器基底＋武器點傷，不可與飾品點傷一起無腦加總；屬性推導需單獨處理武器這件。
- **受擊公式**（敵人攻擊英雄；敵人可暴擊，一般敵人不暴、魔王可暴）：
  ```
  base = 敵攻擊 × (1 − 減傷%)
  base = base − 固定防禦
  敵暴擊時：有效暴傷 = 1 + (敵暴傷 − 1) × (1 − 減暴傷承受%)；base × 有效暴傷
  受傷 = max(1, base)
  ```
  > **上限**：百分比減傷最多 **90%**；暴擊率、local% 等其餘屬性不設上限（待平衡時再視情況加）。
- **每秒回血**：英雄戰鬥中每秒回復 `hpRegen` 點。
- **攻速**：實際攻擊間隔 = 基礎間隔 / (1 + haste)。
- **節奏**：固定 tick（見 §4），各單位用攻擊計時器決定出手。
- **視覺**：Canvas 像素風，英雄左、怪右；攻擊補間、HP 條、跳傷害數字。

### 2.2 生產（Production）

- **無盤面格子**：每種機台一張卡片，卡上控制——「**製造**」（花建造素材，**總台數 +1** 並預設運轉）、「**＋／－**」（配置運轉台數，機台不消失、只在運轉／閒置切換，**不退素材**）。顯示「運轉／總數」X/Y。
- **生產速度 = 基礎速度 × 運轉台數**（閒置機台不耗料、不產出；運轉台數受素材限制即生產速度）。
- 機台運轉：每週期消耗輸入素材、產出中間材料；缺料則閒置（卡片標紅）。
- 3 種機台把 3 種原始素材各自加工成對應中間材料（製裝貨幣）。
- 機台升級（縮週期／增產出）**未實作**（見 §6）；現以「製造更多台」當生產速度軸。
- **視覺**：每張卡顯示機台圖示 ＋ 運轉台數 ＋ 配方 ＋ 進度條 ＋ 製造／－ 鈕。

### 2.3 製裝（Crafting）

- 消耗對應槽的**中間材料**合成裝備（武器吃金屬錠、防具吃皮革、飾品吃精晶；見 §3）。
- **每槽只有一種固定基底**，無階級、無上位替代。基底是小錨，power 全來自詞綴。
- **詞綴 = 隨機數量 ＋ 隨機數值**：
  - 數量每次 roll **1～4 條**，加權偏少、偶爾 4（jackpot）；初版權重 50/30/15/5%（**待平衡**）。
  - 每條詞綴**分 8 階（tier）**，各階有自己的 min/max 與抽中權重（越高階越強、越難中）；抽詞綴時**先依權重抽階、再在該階範圍 roll 值**。從該槽池無重複抽。
    - 初版調性（待平衡）：高階機率**每階約減半**（權重 128→1）；各階範圍**不重疊**（後一階 min 高於前一階 max，整段往上跳）——抽到高階＝明確的數值躍升。敵人曲線已依此重算（§3.2）。
  - **詞綴池固定**（不隨材料變強），單件 power 有上限；成長靠**反覆製裝留最好的**（best-of-N）。
- **製裝機（每槽一台、佇列式）**：製裝**不再瞬發**。武器／防具／飾品各一台製裝機（**起手各 1 台**），比照生產機台——「**製造**」花建造素材**總台數 +1** 並預設運轉、「**＋／－**」配置運轉台數（機台不消失、不退素材）；**製裝速度 = 基礎 × 運轉台數**。
  - 生產分頁中，每個製裝機卡片各自佔一整列，列內直接顯示該槽的**訂單按鈕**與**過濾器**，讓製裝控制集中在同一處。
  - ×1／×10／×100／×1000 按鈕把 N 件加入**該槽製裝佇列**；機台每週期逐件消耗該槽配方材料製裝，**產出走過濾器**（符合進主背包、否則進倉庫）。
  - 佇列空 → 閒置；缺料 → 暫停等料（卡片標紅）。可一鍵**清空佇列**。
  - 建造成本、週期秒數**待平衡**（初版：每台週期 1s、建造 10 中間材料）。
- 裝備穿上即時生效。做壞的可**拆除**，退還該配方 **25% 素材**（小數部分以機率進位，例如 0.75 → 75% 退 1）。
  > 未來：拆除特定詞綴換取高級材料（暫不實作，見 §6）。

### 2.4 研究（Research）

把裝備**拆解成通貨（精髓／結晶）**，再用通貨瞬間升研究階。獨立分頁。（通貨化改造見 2026-06-02 條目。）

- **拆解機**：可製造、以台數計、自動運轉的機器。**速度 = 基礎 × 台數**（每台每週期 2s 取 1 件，待平衡）。每週期自動從**倉庫**取未鎖、非傳奇裝備拆解；無可拆則閒置。
  - **核心「產能」**＝累積達 1 時對當下這件**免費再拆一次**（不消耗裝備）；「退還原料」對拆解機無效。
- **拆解產物（檢查所有拆解配方，產出所有符合者）**：
  - **詞綴精髓（每 stat 一種）**：每條 **T3 以上**非固定詞綴 → 對應 stat 精髓 `+ (tier−2)×10`；T1～T2 不產。
  - **基底結晶（每槽一種：武器／防具／飾品／核心）**：該件基底 → 對應槽結晶 `+1`。
  - 產量刻意對齊舊研究值，使達成各階所需裝備量與通貨化前一致。
- **詞綴研究（每 stat 一條）**：點「研究」**瞬間**消耗 `stageCost`（首階 50、達成後 ×成長）的對應精髓 → 該軌階級 +1，每階**永久 +10% 強度**。
- **基底研究（每槽一條）**：點「研究」瞬間消耗 `baseStageCost`（首階 5、×成長）的對應結晶 → 階級 +1，每階**永久 +20% 基底**（含固定詞綴）。
- 升階時該項目發出閃光。傳奇裝備不被拆解。
- 與手動「拆除」（退 25% 素材）為不同出口。

---

## 3. 遊戲內容（5 區 × 20 關；3 槽固定裝備）

> 「架構」如下；細部數值（敵人曲線、掉率、詞綴範圍、成本）見 `src/game/content.ts` 與 `affixTable.csv`，標**待平衡**者待實測。
> 核心節奏：3 關快速成長 → 魔王牆 → 骰更好的裝突破 → 下一區。素材**不分階**，後期關卡掉量數量級暴增。

### 3.1 素材（3 種，對應 3 槽；無階級）

| 槽 | 原始素材 | 機台 | 中間材料（製裝貨幣） |
|----|----------|------|----------------------|
| 武器 | 礦石 `ore` | 熔爐 | 金屬錠 `ingot` |
| 防具 | 獸皮 `hide` | 鞣製台 | 皮革 `leather` |
| 飾品 | 晶石 `shard` | 晶化器 | 精晶 `crystal` |

- 三種原始素材**所有關卡都掉**，掉量隨關卡編號上升（後期數量級暴增）。
- 無更高階素材；玩家在工廠分配三條產線。

### 3.2 關卡（20 關 = 5 區 × 4 關，第 4 關為魔王）

| 區（關卡） | 區域 | 一般敵人 | 魔王（第 4 關） |
|------------|------|----------|------------------|
| 1（1–4） | 礦坑帶 | 史萊姆 🟢 | 史萊姆王 |
| 2（5–8） | 荒野帶 | 野狼 🐺 | 狼王 |
| 3（9–12） | 晶洞帶 | 晶化魔像 🗿 | 魔像核心 |
| 4（13–16） | 鐵域帶 | 鋼鐵魔偶 🤖 | 鋼鐵霸主 |
| 5（17–20） | 虛空帶 | 虛空獸 👾 | 虛空之王 |

- 每區前 3 關難度漸增、可順順清；**第 4 關魔王＝瓶頸牆**：魔王 HP/ATK **直接指定**（與一般曲線解耦，便於獨立調；初版第 1 區約 10 下可擊殺，**待平衡**），且**擁有暴擊**（一般敵人不暴）。
- **後期魔王相對更厚**以抵銷生產雪球（尖峰倍率逐區加碼，**待平衡**）。
- 一般關：每關 5 波、每波 1 隻。魔王關：前 4 波各 1 隻雜兵暖身、第 5 波單一魔王。
- **掉落**：三種素材皆掉，量隨關卡上升；魔王**保證掉大量素材**當破牆獎勵。

### 3.3 機台（3 種，各 1 進 1 出；數值待平衡）

| 機台 | 建造成本 | 輸入 → 輸出（每週期） | 週期 |
|------|----------|----------------------|------|
| 熔爐 | 5 礦石 | 2 礦石 → 1 金屬錠 | 3s |
| 鞣製台 | 5 獸皮 | 2 獸皮 → 1 皮革 | 3s |
| 晶化器 | 5 晶石 | 2 晶石 → 1 精晶 | 3s |

### 3.4 裝備（3 槽各一固定基底；詞綴 1～4 隨機）

| 槽 | 名稱（暫名） | 固定基底 | 製作成本 |
|----|--------------|----------|----------|
| 武器 | 劍 | atk 10 | 3 金屬錠 |
| 防具 | 甲 | hp 40 / def 3 | 4 皮革 |
| 飾品 | 護符 | critChance 5% | 2 精晶 |

> 基底與成本為**待平衡**初版。基底刻意小，power 主要來自詞綴。

**詞綴池（固定，PoE 風；每槽 5 種）**：

- **武器**：點傷（flat atk）、local 物理%、暴擊、暴傷、攻速
- **防具**：固定生命、固定防禦、每秒回血、百分比減傷、減少承受暴擊傷害
- **飾品**：點傷、暴擊、暴傷、生命、攻速

- 每次製作從該槽 5 種無重複抽 1～4 條（數量加權，見 §2.3）。
- **量產流**：反覆製作、保留各槽最佳、其餘**拆除**退 25% 素材（避免庫存爆量）。
- **背包 / 倉庫分頁**：裝備頁只保留道具檢視，並在上方以子分頁切換**主背包**與**倉庫**；兩者不再同時顯示。逐件按鈕或**右鍵一鍵**互轉。
- **過濾器**：每槽可設任意多列「必備詞綴 ≥ 指定階」條件（AND）；過濾器放在對應製裝機旁，製作時不符者**自動進倉庫**，另有「套用到現有背包」一鍵把現有不符的掃入倉庫。

**詞綴池資料化**：單一事實來源 `src/game/affixTable.csv`（每槽 × 每詞綴 × **每階**一列，欄位 `slot,stat,label,pct,tier,weight,min,max`）。`affixTable.ts` 以 `?raw` 載入解析、匯出 `affixPool(slot)`；要整批重設改 `scripts/gen-affixes.mjs` 規則區（每條詞綴的第 1 階範圍與每階遞增、各階權重）後 `npm run gen:affixes`。

### 3.5 機台與生產速度

- **無盤面**：每種機台一張卡片；製造增加總台數、＋／－ 配置運轉台數（顯示 X/Y），閒置機台不耗料。
- **起手免費附三種機台各一台**（熔爐／鞣製台／晶化器），一開局即有產出。
- **生產速度＝製造更多台**（受素材限制）。機台升級為未來真方案（見 §6）。

---

## 4. 技術架構

- **技術選型**：Vite ＋ TypeScript。介面用 DOM，戰鬥畫面用 Canvas 2D。
- **遊戲迴圈**：`requestAnimationFrame` ＋ 固定步長累加器（fixed timestep accumulator），確保戰鬥/生產 tick 速率穩定、與畫面更新解耦。
- **狀態**：單一 `GameState` 物件（plain data），系統函式對其操作；不引入 ECS。
- **模組切分（初版）**：
  - `combat/` — 波次推進、傷害結算、掉落
  - `production/` — 機台 tick、素材消耗/產出
  - `crafting/` — 配方、詞條 roll
  - `inventory/` — 素材與材料庫存
  - `hero/` — 屬性推導、裝備穿脫
  - `ui/` — DOM 介面（生產盤面、庫存、製裝、關卡選擇）
  - `render/` — Canvas 戰鬥繪製（像素 sprite、HP 條、傷害數字）
  - `save/` — localStorage 存讀檔（定期存檔）
- **存檔**：localStorage，定期序列化 `GameState`。載入時以**現行 schema 深層合併**舊檔遷移（缺欄補預設、動態 record 沿用、型別衝突回退預設），**改版不清檔**；僅解析失敗／損毀才重置。
- **離線收益**：**雛型不做**。關卡僅在「開著頁面時」即時推進。離線結算之後再加。

---

## 5. 雛型範圍（Definition of Done）

雛型完成 = 玩家能完整跑一輪閉環且有感：

- [ ] 選關 → 即時自動戰鬥（像素演出）→ 怪物掉落三種素材進庫存
- [ ] 在 3×3 方格建造 3 種機台、把素材煉成製裝材料
- [ ] 反覆製裝（每槽固定基底 ＋ 1～4 隨機詞綴），保留最佳、丟棄其餘
- [ ] 裝備即時改變戰鬥數值（含新公式：local%、減傷、回血、敵人暴擊）
- [ ] 每區 3 關順爬 → 魔王牆 → 骰更好的裝突破 → 下一區
- [ ] localStorage 存讀檔
- [ ] 體感到「魔王卡住 → 拚生產衝 roll → 突破」的節奏

**不在雛型範圍**：離線/掛機收益、盤面擴建＋機台升級（生產速度真方案）、小隊戰鬥、音效、多英雄、成就/轉生等後期系統。

---

## 6. 待後續決定 / 未實作

- **機台升級**：縮短週期／提高單次產出（目前只能靠製造更多台）。
- **數值平衡**：敵人曲線、魔王尖峰倍率、詞綴範圍、掉落量、製裝成本、詞綴數量權重均為初版，待實測（重點：god 裝能否打穿第 5 區、每區時間是否相近）。
- **global 物理%** 詞綴：攻擊公式已留插槽，詞綴池暫未加入。
- **拆除詞綴換高級材料**：拆除裝備目前只退 25% 素材；未來改成可拆特定詞綴產出高級料。
- **研究擴充**：拆解器速度升級、含前置的研究樹（目前僅「詞綴強度」軌、無前置）。
- 離線/掛機收益結算（時間戳）。
- 美術資產：英雄真正 sprite、敵人像素圖、待機動畫（目前英雄為方塊拼圖、敵人用圖示）。

### 裝備檢視與比較（2026-05-30）

- 滑鼠移上任一裝備（已裝備、主背包、倉庫）時，會顯示浮動 tooltip。
- Tooltip 會拿該裝備與「當前同槽已裝備物」做比較；若該槽未裝備，則只顯示自身數值。
- 比較以「實際生效後的數值」為準，需計入基底研究與詞綴研究加成，不直接照原始 roll 顯示。
- 武器摘要第一行固定顯示「物理傷害」，公式為 `(武器基底攻擊 + 武器點傷) × (1 + 本地物理%)`，並套用研究加成後再顯示。
- Tooltip 比較列需標出正負差值，讓玩家能直接看出升降，不必手算。
### UX 補充更新（2026-05-30）
- 背包頁只保留「主背包」與「倉庫」切換，以及對應裝備列表；製裝訂單與過濾器不再放在背包頁。
- 製裝機恢復為與材料機接近的橫向卡片排列；每張卡片內同時顯示機台控制、製造控制與佇列。
- 材料機與製裝機都使用相同的批次操作概念：主按鈕顯示目前批次與總花費，右側可切換 `1 / 10 / 100`。
- 製裝配方的基底敘述不直接常駐在卡片上，改為提示文字；滑鼠停留時再顯示實際基底效果。
- 過濾器改為 modal 視窗編輯；開啟時覆蓋主介面並阻擋其他 UI 互動。
- 拆解機只有在「有啟用台數」且「倉庫有裝備」時才會累積進度；否則進度條維持 0。
- 基底研究與詞綴研究共用拆解流程：每拆 1 件裝備，會同時增加該槽位的基底研究進度；若裝備上有 T3 以上詞綴，額外提供對應詞綴研究值。
- 拆解機不再限制只能處理有 T3+ 詞綴的裝備；所有倉庫裝備都可拆，但 T1/T2 詞綴仍不提供詞綴研究值。
- 研究頁中，基底研究顯示在詞綴研究上方，並且同樣具有進度條與升階閃爍提示。
- 主畫面的素材列改為底部固定浮動 bar，遊戲進行時持續可見。
- 戰鬥掉落的素材噴字需做短延遲錯開，避免多筆掉落完全重疊。
- 關卡選擇列表需直接顯示該關會出現的敵人，以及各素材的單次掉落範圍，避免只靠 tooltip 或進場後才得知內容。
- 敵方不再有暴擊機制；魔王改以純數值壓力呈現，不使用額外暴傷倍率。
- 原本的「減暴傷承受」詞綴改為「格檔率」。敵人攻擊命中時，若觸發格檔，則在既有百分比減傷上額外加算 `25%` 減傷後再結算固定防禦。
- 格檔成功時，承傷浮字需加上盾牌符號，並稍微淡化，以便和一般受傷區分。
- 詞綴品質權重曲線改為整數權重：最高階固定 `10`，往低階每層乘上 `1.6` 並四捨五入，讓高階詞綴更常見，同時避免小數權重。
### 輪迴系統補充（2026-05-30）
- 輪迴系統：最後一關最終敵人倒下後跳出通關視窗，玩家可選擇繼續當前局，或直接重開一輪並擇一取得永久加成。
- 輪迴加成共有 3 種：研究成長倍率每層乘上 `0.8`，也就是研究門檻改用 `baseCost * (2 * 0.8^輪迴層數)^研究層數`；最終素材掉落量每層乘上 `1.15`；全能力每層乘上 `1.1`。全能力包含生命、防禦、傷害、攻速、暴擊率、暴傷，並在其他來源結算後再乘算。
- 第二輪開始顯示「輪迴」分頁，顯示目前第幾輪，以及三種輪迴加成的累積層數與當前倍率。
- 原料機、製裝機、拆解機的機台成本採階梯式成長：持有數達到 `10`、`100`、`1000` 後，後續每台價格額外提高 `2.5x`；跨門檻批次購買時逐台累加。

### 核心系統補充（2026-05-31）
- 新增核心系統：所有原料機、製裝機、拆解機與核心機都可鑲嵌 2 顆核心；核心與裝備共用背包、倉庫，但屬於獨立道具類型。
- 新增核心機，用於生產核心；核心機本身也可鑲嵌核心，並吃到核心提供的生產速度、產能、返還材料與工藝類效果。
- 核心固定有 1 條「產能」詞綴，會以既有 8 階權重骰值，數值範圍 `2% ~ 4%`；另外再依 `40% / 40% / 20%` 機率骰出 `0 / 1 / 2` 條變動詞。
- 核心變動詞池包含：生產速度、返還材料、提升詞綴階級、增加詞綴數量，以及六種 tag 權重詞（物理、暴擊、速度、生命、防禦、工藝）；其中提升詞綴階級與權重詞數值範圍皆為 `12.5% ~ 100%`。
- 所有詞條都帶 tag；裝備詞與核心詞會受對應 tag 權重影響。增加某類權重的詞本身同時屬於該分類與工藝分類。素材掉落量與返還材料不屬於六大分類。
- 產能機制比照 Factorio：原料機、製裝機與核心機在主進度推進時同步累積紫色細產能條，滿 `100%` 時額外產出 1 個結果，且額外結果必須獨立重骰；拆解機不累積產能條，而是直接按比例提高該次拆解所得研究值。
- 生產速度只作用於機器；返還材料只作用於有材料輸入的機器；提升詞綴階級、增加詞綴數量與權重只作用於會 roll 物品的製裝機與核心機。提升詞綴階級每次只會對該次產物的其中一個詞綴生效。
- 飾品新增「素材掉落量」詞綴；以上新增的固定詞、變動詞與素材掉落量都需進入研究系統、提示文字、比較顯示、存檔遷移與過濾器。
### 解鎖流程更新（2026-05-31）
- 關卡改為循序解鎖。新存檔初始只顯示第 1 關；每次打贏當前關卡後，才會解鎖並顯示下一關。
- 已解鎖關卡只保留在本輪進度中；輪迴重開後，關卡解鎖進度重置回第 1 關。
- 核心系統改為本輪進度解鎖。打贏狼王後，才會開放核心系統。
- 核心系統未開放前，不顯示各機器的核心插槽，也不顯示核心機與核心研究項目。
### 版面與導頁更新（2026-05-31）
- 桌面版右側側頁在開啟時預設佔主畫面約 50% 寬度，左側戰鬥區保留另一半；中小螢幕仍維持既有自適應縮放。
- 地圖分頁中的關卡列表會撐滿側頁剩餘高度，並在該區內自行捲動，不再只顯示固定短列表。
- 當下一關已解鎖時，戰鬥畫面右上角會顯示「前往下一關」按鈕，方便不開地圖也能直接推進。
- 機器上的已插核心可直接 hover 顯示 tooltip，內容沿用核心道具本身的詞綴比較顯示。
- 已插核心的卸下操作改為插槽右上角固定小 `×`，不再額外占用一列按鈕，以避免插槽區排版跳動。
- 核心過濾器新增「任何詞綴」條件；設定為 `任何詞綴 ≥ Tn` 時，只要該核心任一條詞綴達到指定階級就視為符合。
- 本輪首次擊敗尾王時才會跳出通關視窗；若玩家選擇暫不進入下一輪，之後重打尾王不會再次彈出，直到開始下一輪為止。
### 稀有度系統更新（2026-05-31）
- 一般裝備與核心都新增 `normal / magic / rare` 稀有度欄位；核心只會出 `normal` 或 `magic`。
- 一般裝備的稀有度流程：先以 `50%` 從一般升成魔法；若已是魔法，再以 `25%` 從魔法升成稀有。
- 一般裝備詞數改為綁定稀有度：
  - `normal`：`0` 詞
  - `magic`：`1 / 2` 詞，各 `50%`
  - `rare`：`3 / 4` 詞，機率比 `1 : 2`
- 核心保留固定 `1` 條產能詞；其稀有度只決定額外變動詞數：
  - `normal`：固定詞 + `0` 條變動詞
  - `magic`：固定詞 + `1 / 2` 條變動詞，各 `50%`
- 舊存檔讀取時會依現有詞數補齊稀有度，不清檔：
  - 一般裝備：`0詞=一般`、`1~2詞=魔法`、`3~4詞=稀有`
  - 核心：`1詞=一般`、`2~3詞=魔法`
- 核心詞綴「增加詞綴數量」改為「增加稀有度」，只影響稀有度判定機率。
- 「增加稀有度」詞綴數值範圍改為 `1% ~ 8%`。
- UI 顯示稀有度時以底色標示：一般維持原樣，魔法為藍色，稀有為黃色；套用於已裝備、主背包、倉庫與核心插槽/選單。
### 做裝過濾與核心研究更新（2026-05-31）
- 做裝過濾器新增兩種條件，且與原有條件一律採 AND：
  - 變動詞綴數至少 `1 / 2 / 3 / 4`
  - 稀有度至少 `魔法 / 稀有`
- 「詞數」一律指變動詞綴數；固定詞綴不算在內。
- 核心固定產能詞綴不再列入詞綴過濾器選項，也不參與詞綴研究。
- 固定詞綴視為基底的一部分：
  - 只 roll 數值，不分 tier 強化
  - 吃同類型的基底研究加成
  - 不吃詞綴研究加成
- 新增「核心基底研究」：
  - 拆解核心會累積核心基底研究
  - 核心固定產能詞綴由核心基底研究強化
  - 未來若同類裝備新增其他固定詞綴基底，也共用同一條基底研究

### 戰鬥介面與推關流程更新（2026-05-31）
- 右上主分頁順序改為：生產、背包、研究、輪迴；地圖不再放在主分頁列。
- 地圖入口移到戰鬥介面左上角，與「前往下一關」使用同款按鈕樣式。
- 按下地圖後，會以 modal window 顯示已解鎖關卡供玩家選擇。
- 戰鬥介面左下資訊改為兩個子分頁：
  - 裝備
  - 角色數據
  - 預設顯示裝備
- 戰鬥介面右下角新增「自動前往下一關」勾選框。
- 關卡打贏後，戰鬥會暫停 1 秒，再進入下一輪流程：
  - 若未勾自動前往下一關，則 1 秒後重開當前關卡
  - 若已勾自動前往下一關，且下一關已解鎖，則 1 秒後自動切到下一關

### 防禦穿透（2026-05-31）
- 新增戰鬥屬性「防禦穿透」`defPenPct`，作用是無視對方一部分固定防禦。
- 只影響固定防禦 `def`，不影響減傷率、格檔或其他乘區。
- 實際計算方式：
  - 有效防禦 = `def * (1 - defPenPct)`
  - 傷害再用攻擊力扣除有效防禦
- 玩家預設防禦穿透為 `0%`。
- 敵人預設防禦穿透為 `0%`。
- 只有 `2-4` 的狼王防禦穿透為 `50%`。

### 飾品調整（2026-05-31）
- 玩家可同時裝備 `2` 個飾品。
- 飾品基底仍維持原本設計；只有飾品「變動詞綴」數值調整。
- 飾品詞綴表的數值區間改為原本的 `1/4`。
  - 因為可裝 `2` 個飾品，整體上限約等於舊版單飾品詞值的 `1/2`。
### 2026-06-01 變更摘要

- 飾品詞綴與其他部位維持獨立資料表，所有飾品詞綴數值整體提升為原本的 1.5 倍。
- 所有增加最大生命的裝備數值上調為原本 2 倍，包含防具基底生命、防具生命詞綴，以及飾品生命詞綴；若同時屬於飾品詞綴，會先吃飾品 1.5 倍，再吃生命 2 倍。
- 核心詞綴「增加稀有度」數值範圍改為 3%～24%。
- 武器攻速改為「本地攻速」，只作用於武器本身，並與其他來源的全域攻速乘算；飾品上的攻速仍視為全域攻速。
- 背包與倉庫暫時移除手動拆解操作；主背包的批次按鈕改為「全部移至倉庫」。
- 主畫面新增「設定」按鈕，刪除存檔入口移入設定視窗，避免誤觸。
- 做裝過濾器會即時顯示目前設定的平均產出效率，單位為「平均每製作幾件，能得到 1 件符合條件的裝備或核心」。
### 2026-06-01 傳奇核心

- 通關 `2-4` 與 `3-4` 時，各贈送玩家 1 枚傳奇核心。
- 傳奇核心不是一般製造流程的一部分，不能透過核心機或任何正常做裝方式產出。
- 傳奇核心固定帶有 2 條固定詞綴：
  - 一般核心同款的固定產能詞綴
  - `20%` 幸運詞綴階級（原 10%，2026-06-02 上調）：當該核心插在機器上時，該機器做出的每條變動詞綴在決定 tier 時，有 20% 機率改為骰兩次並取較高階級。
- 傳奇核心不帶任何其他隨機變動詞綴。
- 傳奇核心不參與研究：
  - 不提供基底研究
  - 不提供詞綴研究
  - 其固定詞綴也不吃研究加成
- UI 以獨立的傳奇底色顯示，和魔法、稀有分開辨識。

### 2026-06-01 生產系統大改造（組裝機 / 連續生產）

> 本節為生產／製裝的**最新權威規格**，取代 §2.2 生產、§2.3 製裝、§3.3 機台、§3.5，以及先前「製裝機佇列」「拆解機」相關敘述中衝突的部分。核心理念：把「點擊做裝」改成「規劃生產線」——所有機台統一為單一「組裝機」，玩家在生產分頁的行裡放置組裝機並指定配方，機台連續運轉、自動吃料產出。

**組裝機（唯一機台）**
- 是需要「生產出來」的道具，以**庫存台數** `spareAssemblers` 計。乾淨存檔開局送 **1 台**、未放置。
- 放置：生產分頁每個子分頁有可點擊的空行；點空行 → 跳配方視窗選配方 → 消耗 1 台庫存組裝機放入該行。
- 行內以 **－／＋** 增減機器數量，每次加減量為一個**全域步進值**（十的冪次、最低 1，旁邊 `/10`、`×10` 調整）。＋ 受庫存組裝機限制；－ 退回庫存且**最少保留 1 台**（要整行收回用行右上 **✕**，台數退回庫存、行清空）。
- 每行可切換**運轉／暫停**（開關在 ✕ 左側：運轉＝綠底 ▶、暫停＝紅底 ⏸）；暫停的行不耗料、不產出、進度歸零。
- 每行 **2 個核心插槽**（掛在「行」上，效果套用整條線）。
- 速度 ＝ 基礎週期 ÷（該行台數 ×（1＋核心加速%））；**產能**條掛在行上，滿 100% 額外產出 1 件（獨立重骰、不回灌，沿用已修正邏輯）。
- **自動製造／手動下單（每行可切換）**：行的右半為訂單區，左半為機台控制。
  - **自動製造**（預設）：連續運轉，每週期吃輸入、產出 1 件；缺料暫停（行標紅）。此時訂單區灰掉不可互動。
  - **手動下單**：只生產訂單佇列裡的數量（×1／×10／×100／×1000 加單、可清空），做完即停；無訂單時不耗料、不預留料（會退回已預留的料）。
- **原料公平發料（逐項＋可部分累積）**：每 tick 先做發料——所有分頁的行**共用同一全域料池**；對**每一種原料**各自輪流發給需要它的行（運轉中＋已選配方＋未暫停），每行該項每輪最多補「該項一次量」但**可只拿到目前庫存有的量（部分累積）**，存到上限 `台數 × 該項一次量`；循環直到庫存不足或都滿。**逐項＋部分累積**讓大用量配方（如組裝機 ore+hide+shard 各 8）能一點一點把每種料攢滿，不會被只要 2 的小用量線（冶煉／揉製）把該原料一直壓在門檻下而永遠拿不到。每 tick 起點輪替避免前段偏袒。行只用自己預留的緩衝運轉，且**各項都湊滿一次運轉量才開工**。預留量不顯示於 UI；刪除機器／刪分頁／換配方會退回預留原料。
  - 註：若多輸入配方有某項原料完全沒供應，該行會持有已拿到的其他原料（與同吃該料的線輪流分享，不會完全餓死對方）；暫停或收回該行即可釋放。

**配方 grid（放置／變更配方時以視窗呈現，grid 排列）**

| 配方 | 輸入（每週期） | 產出 | 解鎖 |
|------|----------------|------|------|
| 熔煉 | 2 礦石 | 1 金屬錠 | 開局 |
| 鞣製 | 2 獸皮 | 1 皮革 | 開局 |
| 晶化 | 2 晶石 | 1 精晶 | 通關 1-4 |
| 武器 | 3 金屬錠 | 劍（裝備） | 開局 |
| 防具 | 4 皮革 | 甲（裝備） | 開局 |
| 飾品 | 2 精晶 | 護符（裝備） | 通關 1-4 |
| 核心 | 見核心配方 | 核心 | 通關 2-4 |
| 組裝機 | 8 礦石 ＋ 8 獸皮 ＋ 8 晶石 | 庫存組裝機 +1 | 開局 |
| 研究室 | 8 金屬錠 ＋ 8 皮革 ＋ 8 精晶 | 研究室 +1 | 開局 |

- 未解鎖配方在 grid 中不可選。週期與成本為**待平衡**初版。

**產物流向（過濾器為每行獨立）**
- 會 roll 物品的配方（武器／防具／飾品／核心）：該行附**一個過濾器**（鈕在配方名稱右側），決定產物進主背包或倉庫（符合進背包、否則進倉庫）。
- 提煉／組裝機／研究室配方：產出素材或機台數，無過濾器。

**過濾規則 UX（機器行過濾器與背包整理共用同一套編輯器）**
- 一行一條規則；每條可選**至少（≥）／至多（≤）**並就地改參數（不必刪掉重建）。
- 規則種類：某詞綴階級、變動詞綴數、稀有度（皆支援至少／至多）。
- 編輯器首行固定說明（較大字）：「符合此規則的物品會被保留，其餘會回收至倉庫。」

**背包整理（與生產過濾器各自獨立）**
- 背包頁「整理背包」鈕：依**每類型一組**的整理規則，把現有主背包不符的道具收進倉庫（空規則＝全留；上鎖物不動）。
- 整理規則**每類型一組**（武器／防具／飾品／核心），在背包選定該類型分頁時才顯示其規則編輯器；與機器行的生產過濾器是**獨立**的兩套。

**研究室（取代拆解機）**
- 由組裝機「研究室」配方生產，以**台數**計；顯示於研究分頁，只有「運轉／停」開關與 **2 核心插槽**。
- 行為照舊：運轉時每週期從倉庫取裝備銷毀 → 基底研究 ＋（T3+）詞綴研究；速度 ＝ 基礎 × 運轉台數；產能核心提高該次研究值（沿用現有邏輯）。
- 乾淨存檔開局 **0 台**（需自行生產）。

**解鎖與掉落**
- 三種原料（礦石／獸皮／晶石）**所有關卡照舊都掉**（晶石需保留以加開組裝機）。
- 晶化配方、飾品配方：**通關 1-4 後**解鎖。
- 核心配方：通關 2-4 後解鎖（不變）。
- 傳奇核心贈送改為**通關 2-4 與 4-4**（原 3-4 移到 4-4；覆寫前一節 §傳奇核心的 3-4）。

**新手引導**
- 乾淨存檔首次進入：預設顯示生產分頁，第一個空行**閃爍**提示放置；放下第一台機器後停止閃爍。

**移除**
- 機台階梯成本（10/100/1000 ×2.5）取消，配方固定單價。
- ×N 製裝訂單佇列移除。
- 舊「每槽全域過濾器」改為**每行過濾器**。

**子分頁**
- 生產分頁含多個子分頁；玩家可自行**加開**分頁、改名、刪除（刪除把該頁機器退回庫存）。空行數、分頁數皆不限。

**存檔遷移（不清檔）**
- 素材、研究、基底研究、背包／倉庫道具、輪迴、解鎖進度全保留。
- 舊生產機台 ＋ 製裝機 ＋ 核心機台數總和 → **庫存組裝機數**；舊拆解機台數 → **研究室數**。
- 舊機台上已插核心 → 退回倉庫（不遺失）。
- 生產佈局（行／分頁）重置為空，由玩家重擺。
### 符文系統（2026-06-02）

- 左下角色區子分頁順序改為：`裝備 / 符文 / 角色數據`。
- 新增單一符文槽配置系統。玩家每輪開局直接擁有 2 個符文，但同時間只能配置其中 1 個。
- 符文配置屬於本輪狀態；輪迴後會重置，要重新選擇。
- 符文 UI 使用接近核心槽的方形視覺，並以北歐符文字形作為圖案。
- 目前開放的兩個符文如下：
  - `狂戰符文`：每失去 `25` 生命，獲得 `10%` 更多攻速。此效果為獨立乘區，與其他所有攻速來源相乘，但同符文自己的層數彼此相加。副作用是最終 `hpRegen` 減半。
  - `再生符文`：每秒回復 `3%` 最大生命。副作用是格擋時的額外減傷從 `25%` 降為 `12.5%`。
- 角色數據顯示必須即時反映符文效果；狂戰符文的攻速會隨當前生命變動即時更新。
- 固定點傷與 `atk` 詞綴改為最小／最大傷害區間。武器基底現在直接定義為 `atkMin / atkMax`；`atk` 詞綴也不再是單一點傷，而是像 `+2~4` 這種區間加成。角色面板與戰鬥實際傷害都以該總區間為準，每次攻擊時在區間內隨機抽一次傷害。
  - 存檔遷移：舊武器基底以單一 `atk` 欄位儲存者，載入時轉成 `atkMin = atkMax = atk`；否則新公式（`deriveStats`／武器 DPS）讀不到 `atkMin/atkMax`，基底傷害會歸零（曾導致舊武器 DPS 顯示 0、實戰基底傷害也消失）。

### 做裝過濾估算與裝備顯示更新（2026-06-02）

- 過濾器估算改用蒙地卡羅取樣 `100000` 次，文案為「成功率 `X%`（模擬生產 `N` 次中，可得到 `Y` 件被保留的〇〇）」。只有成功率百分比高亮放大，括號內維持正常字級。命中 0 件顯示 `0%`；未設規則則顯示產物全進主背包。
- 取樣為估計值、每次重開會有統計誤差屬正常；計算時核心效果與加權詞綴池只在迴圈外準備一次（效能）。
- 裝備詞綴版面統一為：基底 → 固定詞綴 ─ 分隔線 ─ 其餘變動詞綴（依 stat 字母排序）。固定詞綴永遠在分隔線之上。武器的「物理 DPS」放在**最下面**（再以一條分隔線與詞綴隔開），讓詞綴本體排列與其他道具一致。
- **武器物理 DPS**：`平均物理傷害 × 每秒攻擊次數 × 暴擊期望係數`。以「預設英雄基底 ＋ 該武器的攻速／暴擊詞綴（含研究加成）」估算，計入暴擊與攻速，**不含**符文、技能、轉生 power 等 buff。字體較大較粗，不受精簡面板（隱藏研究加成 %）影響、永遠顯示。

### 核心強化（2026-06-02）

- 傳奇核心幸運詞綴階級 `10% → 20%`。
- **詞綴升階機率**（核心詞綴 `upgradeTierChance`，敘述由「提高詞綴階級」改為「詞綴升階機率」）改寫機制：
  - 舊：每件製作只判定一次，命中則隨機挑一條詞綴升一階。
  - 新：**每條詞綴的每次 tier roll 各自判定一次**，命中則該次 roll 的階級 +1（不超過最高階）。
  - 與幸運詞綴階級疊加時，幸運的兩次 tier roll **各自分別**做升階判定，再取較高階級。
  - 實際製作（`crafting.ts`）與過濾器估算（`craftingEstimate.ts`）使用相同邏輯，估算才會準確反映。
  - 因每詞每骰判定後威力放大，`upgradeTierChance` 值範圍**減半**：`tiers(0.125,1)` → `tiers(0.0625,0.5)`。存檔遷移（`SAVE_VERSION` 11、僅 `priorVersion < 11` 跑一次）把既有核心已 roll 的該詞值減半，避免每次載入重複減半。（原訂 v10，因開發期 HMR 時序使存檔在減半碼上線前先被標記 v10，改用 v11 重跑一次。）

### 點傷與敵方數值同步加倍（2026-06-02）

- 所有**點傷（flat atk）×2**：英雄裸值 `atkMin/atkMax`、武器基底、武器與飾品的 `atk` 詞綴（`affixTable.csv`）。
- 敵方**血量 ×2**（`E_HP`、`BOSS_HP`）。
- 為維持戰鬥中性，敵方**防禦也 ×2**（`E_DEF`）：防禦是減法，若只倍攻不倍防，`2·atk−def ≠ 2·(atk−def)`，有防禦的敵人會變好打。
- 存檔遷移（`SAVE_VERSION` 12、僅 `priorVersion < 12` 跑一次）把既有裝備已 roll 的點傷（基底 atkMin/atkMax 與 atk 詞綴）一併 ×2，否則舊裝點傷未倍、敵方血/防雙倍會使戰力腰斬。
- 敵方攻擊、英雄生命不變；因擊殺所需打擊數不變、戰鬥時長不變，英雄受擊總量亦不變。
- 殘留非中性僅在「玩家點傷 ≈ 敵防禦」之極端低傷情境（`max(1,…)` 下限與每擊四捨五入造成 ±1 擊），正常遊戲可忽略。

### UI 檢視狀態持久化（2026-06-02）

- 分頁與子分頁的瀏覽狀態存於 `localStorage`（`forge-loop-view`，與遊戲存檔分開），刷新後沿用：右側抽屜分頁、左下資訊子分頁（裝備／符文／角色數據）、背包子分頁（主背包／倉庫）、背包過濾子分頁、生產子分頁、配方選單子分頁。

### 戰鬥中換裝／符文延後生效（2026-06-02）

- **戰鬥中**（有存活敵人、非結算暫停、非通關待續）更改裝備或符文時，不立即生效，而是排入待辦佇列（`state.pendingLoadout`）。
- 待辦於**下場戰鬥開始**時套用：`startStage`（手動選關、自動前往、結算後重啟）與 `heroDeath`（戰敗重來）的開頭呼叫 `applyPendingLoadout`，依序執行 equip／unequip／符文選擇後清空。
- **非戰鬥中**（如關卡結算空檔）更改則立即生效。
- 戰鬥本身與數值推導（`deriveStats`）不變；僅在戰鬥邊界替換 `state.equipped`／`state.runes.selected`。
- **待辦規則**：
  - 裝備：**依槽位分開排隊**——武器／防具各保留一個（新選覆蓋同槽待穿）；飾品有兩槽，最多保留兩個（超過丟最舊）。先預排卸下兩個飾品、再依序選兩件飾品，即可分別排入兩個空出的槽。
  - 卸下：可預排**多個**槽；再點同一槽＝取消該槽預排（toggle）。
  - 符文：只保留最後一次選擇。
  - 套用固定順序：**先全部卸下 → 再依各待穿的目標槽穿上（`equipIntoSlot` 精準放入）→ 再符文**。
  - 飾品目標槽**在排入當下就決定並固定**（存於 equip 動作的 `slot`，`queueLoadoutAction`）：第一件取空槽否則飾品 1，第二件補另一格，第三件取代最舊。**之後卸下其他槽不會重算既有待穿的目標槽**（修正先前動態重算導致「卸下飾品 2 會把飾品 1 的取代搶走」的問題）。所以連續預排兩件飾品分別落在兩格。`resolvePendingSlots` 只讀已存的目標槽並算出被取代的槽。
- UI：待套用的道具卡／裝備格蓋半透明暗色遮罩，中央顯示無框文字（「下場戰鬥套用」／「下場卸下」）＋紅色「取消」鈕；遮罩同時擋住該卡的其他操作。
- **連動**：預排穿上某裝備時，其目標槽若已有裝備，該槽也顯示「下場卸下」（被取代）。取消待穿或取消該槽卸下，會連動取消另一個（`cancelPendingEquip`／`cancelPendingVacate` 互相清除對應動作）。目標槽解析見 `resolvePendingSlots`（飾品優先取未被預排的槽）。
- **待穿裝備鎖定**：待套用期間該裝備不可移入倉庫、不可刪除、也不被「整理背包」清除（`toWarehouse`／`discard`／`organizeBag` 皆跳過待穿 uid）。
- 符文格同時標示兩種狀態、用不同色框區分：**正在套用**（當前生效）為紫色實心強調、最明顯；**下場套用**（戰鬥中預排）為金色虛線框、明顯度較低。戰鬥中改符文時兩者並存（原符文續標「正在套用」，新選標「下場套用」）。「當前符文」插槽顯示實際生效的符文。
- 待辦邏輯集中於 `loadout.ts`；`pendingLoadout` 存入存檔（舊檔缺欄位自動補空陣列）。

### 背包排序（2026-06-02）

- 背包介面（過濾列下方）新增排序列：能力值下拉 ＋ 遞增／遞減切換鈕。
- 排序設定**依分類（全部／武器／防具／飾品／核心）各自記憶**，存於 `forge-loop-view` 的 `bagSort`，刷新不變；主背包與倉庫共用同分類設定。
- 可排序鍵依分類提供：武器含**物理 DPS**、點傷、暴擊率…；防具含生命／防禦…；飾品含點傷／暴擊…；核心含產能／各權重…；另各分類皆有「稀有度」「變動詞綴數」。
- 能力值排序取「**基底＋全部詞綴相加後的生效值**」（含研究加成）。例如飾品暴擊率＝基底固定值＋暴擊率詞綴合計；武器物理 DPS 以 `getWeaponPhysicalDps` 計。`atk` 取最小／最大平均。
- **固定詞綴無 tier 概念**：固定詞綴（如核心產能、傳奇核心的幸運）只在單一數值範圍內 roll 值，不分階、不加權。
  - `CORE_FIXED_AFFIX`（產能）改為單一範圍 `[0.02, 0.04]`；一般核心的產能值改為該範圍內均勻 roll（平均略升）。
  - 顯示：固定詞綴右側標示「固定」，**不再顯示 `T#`**。
- **傳奇核心產能固定詞永遠為範圍最高 roll**（即 `0.04`）。
- 存檔遷移：既有傳奇核心會在載入時對齊現行規格（幸運 `0.2`、產能最高 roll）。

### 戰鬥介面可隱藏（2026-06-02）

- 戰鬥面板右上角新增「隱藏戰鬥／顯示戰鬥」切換鈕（永遠可見）。
- 隱藏時收起整塊戰鬥畫布區（含畫布與疊於其上的地圖／前往下一關／自動前往控制），空間讓給下方資訊面板與右側抽屜；需要地圖等控制時再切回顯示。
- 戰鬥邏輯（掉落、推關、生產）照常於背景運行，但停止畫布繪製，且**不產生任何浮動文字／攻擊特效**（傷害數字、素材掉落數字等），以省效能。
- 切換狀態存於 `localStorage`，重整後保留。

### 研究通貨化＋拆解機（2026-06-02）

- 研究從「直接吃裝備」改為「吃**拆解通貨**」：
  - **詞綴精髓 `essences`（每 stat 一種）**：拆裝備時每條 T3+ 非固定詞綴產 `(tier−2)×10`；詞綴研究消耗之。
  - **基底結晶 `crystals`（每槽一種）**：拆裝備時該件基底產 `+1`；基底研究消耗之。
  - 產量對齊舊研究值 → 達各階所需裝備量與以前一致。
- **新拆解機 `state.dismantler`**：以台數計、自動從倉庫拆未鎖非傳奇裝備 → 精髓／結晶。由「拆解機」配方（組裝機生產、成本同舊研究室）製造。核心「產能」＝累積達 1 對當下這件免費再拆（不消耗）；「退還原料」無效。
- **研究改瞬間**：點「研究」即足量時扣通貨、階級 +1、發出閃光；不再累積時間或 points。`research.points`／`baseResearchPoints` 退役（保留欄位），已完成 `stages`／`baseResearch` 保留。
- **研究室停用保留**：`state.lab` 與 `lab` 配方保留供存檔相容，但不 tick、不顯示於任何 UI、不與任何東西作用（有機會再移除）。
- **存檔遷移**（`SAVE_VERSION` 14、`priorVersion < 14`，冪等）：既有研究室 → **等量拆解機**（核心一併移到拆解機、lab 歸 0 inert）；殘餘研究值（points／baseResearchPoints）兌換成對應精髓／結晶。（原訂 v13，因開發期 HMR 時序提早標記而改用 v14 重跑。）
- UI：研究分頁改顯示拆解機（台數／進度／核心槽）＋各研究軌的庫存通貨、下一階成本、可研究時亮起的「研究」鈕；底部素材列 🔬研究室 → 🪓拆解機。

### 存檔匯出／匯入（2026-06-02）

- 設定視窗新增「匯出存檔」「匯入存檔」。
- 匯出：序列化目前狀態，優先用 File System Access API（`showSaveFilePicker`）讓玩家選路徑存到本機，不支援時退回瀏覽器下載（檔名 `forge-loop-save-<時間>.json`）。
- 匯入：選檔 → 驗證 JSON → 確認覆蓋 → 寫入存檔位置並 `location.reload()`，走正常 migrate 流程載入。
- 用途：改版/遷移有風險時先備份，避免存檔失敗無法復原。`save.ts` 以 `exportSaveText`／`importSaveText` 封裝、不外露儲存 key。
